# УИ-ТЭЦ — Оперативный Портал Дежурного Персонала
Рабочее веб-приложение для сотрудников ТЭЦ: авторизация, профили, сменный состав, рабочие места, оборудование и ролевой доступ.

Проект построен на **Vite + React + Tailwind + Supabase (Postgres + Auth + RLS + Realtime + Storage)**.

---

## 🚀 Цели проекта
Создать единый внутренний портал для оперативного персонала, где сотрудник может:
- войти под своим Supabase-аккаунтом;
- пройти онбординг (привязка к записи `employees`);
- увидеть свою текущую смену, роль, точку управления (ЦТЩУ);
- просматривать оборудование, закреплённое за его щитом;
- начальник смены может редактировать состав смены;
- все данные защищены **Row Level Security (RLS)**.

---

## 📦 Технологии и данные

- Frontend: Vite + React + React Router + Tailwind CSS, Supabase JS SDK.
- Auth: Supabase Auth.
- Таблицы (Supabase):
  - `profiles` (id=auth.uid, full_name, department, role_id) → внеш. ключ на `roles`
  - `roles` (id: operator|supervisor|admin, name, description)
  - `employees` (first_name, last_name, middle_name, position_id → positions.id, control_point, auth_user_id)
  - `positions` (справочник должностей)
  - `equipment` (основной реестр)
  - `roster` (состав смены)
  - `orders` (журналы КТЦ: unit='ktc', section='docs', type: admin|turbine|boiler|daily, title, body, author_name, control_point)
- RLS: включен для profiles/employees/orders (доступ к своим записям, оператор/супервизор/админ — по ролям).
- Deployment: GitHub + Vercel (CI/CD). Env: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`.

---

## 🏗 Архитектура приложения

```
Frontend (React)
├─ LoginPage
├─ StartPage (профиль, погода, роль, смена)
├─ RosterPage (сменный состав)
├─ EquipmentPage (оборудование по control_point)
└─ Hooks (useAuth, useEmployeeProfile, useRosterAssignments)

Supabase
├─ Auth
├─ employees / roles / workplaces / equipment
├─ RLS правила
├─ Views + функции
└─ Storage (аватары)

Deploy
├─ GitHub
├─ Vercel
└─ Автодеплой при push в main
```

---

## 🧱 Установка и запуск локально

```bash
cp .env.example .env.local # заполнить VITE_SUPABASE_URL и VITE_SUPABASE_ANON_KEY
npm install
npm run dev
```

Тестовый билд:

```bash
npm run build
```

🌐 Деплой на Vercel

Подключить GitHub-репозиторий.

Добавить env переменные:

```
VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY
```

Deploy.

Приложение доступно по адресу:
https://your-project.vercel.app

📌 Статус разработки (MVP DEMO)

- Dev-среда: Vite + React + Tailwind + Supabase SDK (Hello, TPP Portal)
- Авторизация (Auth)
- Профиль сотрудника
- Просмотр смены
- Просмотр оборудования
- Редактирование состава смены
- Мини-стилизация
- Тестирование RLS
- Завершённый демо-вариант

📄 Лицензия

Внутренний проект. Распространению не подлежит.

---

# 🟩 **Файл PROGRESS.md — ТВОЙ КОНТРОЛЬ ПРОЕКТА (в корень репо)**

```md
# Прогресс разработки (48-часовой штурм)

## 🟥 Backlog (ещё не сделано)
- [ ] Очистить структуру проекта (Vite + React)
- [ ] Подключить Tailwind
- [ ] Подключить Supabase SDK
- [ ] Проверить таблицы в БД
- [ ] Настроить RLS (operator / supervisor / admin)
- [ ] Триггер on_auth_user_created
- [ ] LoginPage
- [ ] StartPage
- [ ] RosterPage
- [ ] RosterModal (редактирование смен)
- [ ] EquipmentPage
- [ ] useEmployeeProfile hook
- [ ] useRosterAssignments hook
- [ ] useEquipment hook
- [ ] GitHub репозиторий
- [ ] Деплой на Vercel
- [ ] Стайлинг (Header / Sidebar)
- [ ] Логотип УИ-ТЭЦ
- [ ] Тестирование ролей
- [ ] Тестирование в продакшене

---

## 🟨 In Progress (в работе)
*(отмечай то, что делаешь сейчас)*  
- [ ] …

---

## 🟩 Done (готово)
*(перетаскивай задачи сюда, чтобы видеть прогресс)*  
- [x] Создан файл README.md  
- [x] Сформирована Kanban-доска  
- [x] Стартовый рабочий план утверждён  

🔥 ОКЕЙ, ЧТО ДЕЛАЕМ ПРЯМО СЕЙЧАС
```
